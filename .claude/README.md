# .claude 目录说明

本目录包含 shadcn-admin 项目的 Claude Code 配置文件和工具脚本。

## 📁 目录结构

```
.claude/
├── settings.json              # 主配置文件 - Claude Code 权限和钩子设置
├── settings.local.json        # 本地配置 - 环境变量和本地权限（不提交到 Git）
├── CLAUDE.md                  # 项目指导文档 - 为 Claude Code 提供项目上下文
├── .gitignore                 # Git 忽略文件
├── hooks/                     # 钩子脚本目录
│   ├── pre-command.js         # 命令执行前钩子
│   ├── post-command.js        # 命令执行后钩子
│   ├── pre-commit.js          # Git 提交前钩子
│   ├── post-build.js          # 构建后钩子
│   └── pre-format.js          # 格式化前钩子
├── scripts/                   # 工具脚本目录
│   ├── dev-setup.js           # 开发环境设置脚本
│   ├── quality-check.js        # 代码质量检查脚本
│   └── cleanup.js             # 项目清理脚本
└── commands/                  # 自定义命令目录
    ├── dev.js                 # 增强版开发服务器启动命令
    └── build.js               # 增强版构建命令
```

## ⚙️ 配置文件说明

### settings.json
- 定义 Claude Code 的权限控制
- 配置钩子脚本
- 设置自定义命令
- 环境变量配置

### settings.local.json
- 本地开发环境配置
- API 密钥和敏感信息
- 个人权限设置
- **注意：此文件不提交到版本控制**

### CLAUDE.md
- 项目技术栈说明
- 开发工作流程指导
- 架构模式说明
- Claude Code 使用建议

## 🔧 钩子脚本功能

### pre-command.js
- 命令执行前的日志记录
- 项目健康检查
- 环境状态验证

### post-command.js
- 命令执行结果记录
- TypeScript 错误检查
- 构建状态报告

### pre-commit.js
- Git 提交前代码质量检查
- 运行 ESLint 和 TypeScript 检查
- 可选的格式化验证

### post-build.js
- 构建结果分析
- 包大小检查
- 构建报告生成

### pre-format.js
- 格式化前环境检查
- Prettier 配置验证
- 格式化建议

## 🛠️ 工具脚本功能

### dev-setup.js
- 开发环境初始化
- 依赖检查和安装
- Node.js 和 pnpm 版本验证
- Git 仓库设置

### quality-check.js
- 全面代码质量检查
- TypeScript、ESLint、格式检查
- 项目结构分析
- 质量报告生成

### cleanup.js
- 项目清理工具
- 删除构建产物和缓存
- 包管理器缓存清理
- 磁盘空间回收

## 🚀 自定义命令功能

### dev.js
- 增强版开发服务器启动
- 环境检查和依赖验证
- 端口占用检测
- 开发提示信息

### build.js
- 增强版构建命令
- 构建前质量检查
- 构建结果分析
- 性能优化建议

## 📋 使用方法

### 1. 设置开发环境
```bash
node .claude/scripts/dev-setup.js
```

### 2. 运行代码质量检查
```bash
node .claude/scripts/quality-check.js
```

### 3. 启动开发服务器
```bash
node .claude/commands/dev.js
```

### 4. 执行构建
```bash
node .claude/commands/build.js
```

### 5. 清理项目
```bash
node .claude/scripts/cleanup.js
```

## 🔄 Git 集成

钩子脚本已配置到 `settings.json` 中，会在以下时机自动执行：

- **每次命令执行前/后**: `pre-command.js`, `post-command.js`
- **Git 提交前**: `pre-commit.js`
- **项目构建后**: `post-build.js`
- **代码格式化前**: `pre-format.js`

## 📝 自定义配置

### 添加新权限
编辑 `.claude/settings.json` 的 `permissions.allow` 数组：

```json
{
  "permissions": {
    "allow": [
      "Bash(your-custom-command:*)",
      "Read(your-custom-files)"
    ]
  }
}
```

### 添加新钩子
1. 在 `hooks/` 目录创建脚本
2. 在 `settings.json` 中配置

```json
{
  "hooks": {
    "yourHook": "./hooks/your-hook.js"
  }
}
```

### 添加新命令
1. 在 `commands/` 目录创建脚本
2. 在 `settings.json` 中配置

```json
{
  "customCommands": {
    "your-command": "node .claude/commands/your-command.js"
  }
}
```

## 🔒 安全注意事项

- `settings.local.json` 包含敏感信息，不提交到 Git
- API 密钥和令牌存储在本地配置文件中
- 权限配置遵循最小权限原则
- 危险命令需要用户确认

## 📞 支持

如有问题或建议，请：
1. 检查 Claude Code 官方文档
2. 查看项目 CLAUDE.md 指导
3. 提交 Issue 或 Pull Request

---

**注意**: 此配置专为 shadcn-admin 项目定制，包含 React 19 + TypeScript + Vite 技术栈的特定优化。